<div class="product-detail">
  {{#with product}}
    <img src={{thumbnail}} alt="" />
    <div class="product-text">
      <h1>{{title}}</h1>
      <p>Precio: ${{price}}</p>
      <p>{{description}}</p>
    </div>
  {{/with}}
</div>